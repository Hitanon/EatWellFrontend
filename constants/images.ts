import { ImageSourcePropType } from "react-native";

const images: { [key: string]: ImageSourcePropType } = {
  // food
  foodPlaceholder: require("../assets/images/food-placeholder.png"),
};

export default images;
