export enum InputType {
  TEXT = "text",
  NUMERIC = "numeric",
  DECIMAL = "decimal",
  PHONE = "phone",
}

export enum TabType {
  Meals = "Блюда",
  Products = "Продукты",
}

export enum TabProductType {
  Meal = "Блюдо",
  Product = "Продукт",
}

export enum CreateProductTabType {
  SCANNER = "Сканер",
  PHOTO = "Фото",
  MANUAL = "Вручную",
}

export enum WaterValueType {
  v150 = 150,
  v200 = 200,
  v300 = 300,
  v500 = 500,
  v1000 = 1000,
}
